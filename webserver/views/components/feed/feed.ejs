<% feed.results.forEach(post => { %>
    <div id="<%= post.id %>" class="post">
        <div class="row" style="position:relative; padding-right: 130px">
            <img class="fotoUsuarioPost" src="<%= post.urlImgUsuario %>">
            <h4><%= post.nome %></h4>

            <div style="position:absolute; right: 0;">
                <!--
                <% /*if (post.userLikes.includes(usuario._id)){ %>
                    <button class="btn-memeIO btn" onclick="avaliarPost('<%= post._id %>','<%= usuario._id %>', this)">
                        <i class="fas fa-laugh-squint fa-lg liked"></i>
                    </button>
                <% }else { %>
                    <button class="btn-memeIO btn" onclick="avaliarPost('<%= post._id %>','<%= usuario._id %>', this)">
                        <i class="fas fa-laugh-squint fa-lg"></i>
                    </button>
                <% };*/ %>
                -->
                <button class="btn btn-memeIO" data-toggle="modal" data-target="#denunciarPublicação" data-idPost="<%= post.id %>" onclick="associarIdPost(this)">
                    <i class="fas fa-flag"></i>
                </button>
                <% if (usuario._id == post.actor.substring(5) || usuario.adm == 1) { %>
                    <button class="btn btn-memeIO" onclick="deletarPost('<%= post.id %>')">
                        <i class="fas fa-trash-alt"></i>
                    </button>
                <% } %>
            </div>
        </div>
        <div class="row">
            <p style="width: 100%;"><%= post.conteudo%></p>
            <img src="<%= post.url %>">
        </div>

    </div>
<% }) %>